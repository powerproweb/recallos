export default function Search() {
  return (
    <div>
      <h1 style={{ fontSize: "1.4rem", marginBottom: "1rem" }}>Search</h1>
      <p style={{ color: "#8b949e" }}>
        Hybrid search (ripgrep + FTS5 + semantic) — coming in Phase 1.5.
      </p>
    </div>
  );
}
